console.log("array basics")

// Check if given value is array in javascript

const a="sujata"
const checking=Array.isArray(a);
console.log(checking)

const arr=[1,2,3,4]
const isArray=Array.isArray(arr);
console.log(isArray)